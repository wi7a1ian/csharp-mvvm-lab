<UserControl x:Class="MvvmSampleApp.Views.FontSizeChangeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MvvmSampleApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding FontSizeChangeViewModel, Source={StaticResource Locator}}">
        <StackPanel Margin="10">
            <Label>Change friend's font size:</Label>
            <TextBlock FontStyle="Italic">(via mediator service)</TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="+" Width="30" Margin="0,0,5,0" Command="{Binding ChangeFontSizeCommand}" CommandParameter="+" />
                <Button Content="-" Width="30" Command="{Binding ChangeFontSizeCommand}" CommandParameter="-" />
            </StackPanel>
            <TextBlock FontStyle="Italic"><Run>Font size:</Run> <Run Text="{Binding ActiveFontSize}" /></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
